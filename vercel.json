{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/green-valley run build",
  "outputDirectory": "artifacts/green-valley/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
